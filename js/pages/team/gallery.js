(function (App) {
  'use strict';

  function init(options) {
    if (!options || !options.button) return;
    console.warn('[team/gallery] TODO: to be implemented');
  }

  App.teamGallery = { init: init };
})(window.App);
